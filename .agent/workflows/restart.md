---
description: stop, start and restart project services (db, backend, frontend) in separate processes
---

This workflow provides a single command to perform a clean restart of all services.

// turbo-all
```bash
./scripts/restart_all.sh
```