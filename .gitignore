node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# SvelteKit static export (adapter-static → dist/); regenerate with npm run build
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Backend binaries (local go build; CI uses its own output name)
backend/sz-gugel-bin
backend/backend

# Local dev: PID files and logs (scripts/restart_all.sh, etc.)
server.pid
backend/server.pid
*.log

# Extension Build Output
extension/
